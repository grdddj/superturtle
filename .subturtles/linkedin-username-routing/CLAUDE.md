## Current Task
All backlog items completed.

## End Goal with Specs
- App supports profile route `/:username` (React Router).
- Profile page loads user by username (Convex query).
- Navigation supports direct URL entry.

## Backlog
- [x] Add query `getUserByUsername` usage in frontend; ensure exists in `linkedin-demo/src/convex/users.ts`.
- [x] Implement routing in `linkedin-demo/src/App.js` (React Router) with `/:username` path.
- [x] Update `Profile` to accept username param or resolved userId.
- [x] Commit: `add username profile route`

## Notes
- Keep existing view switching functional (feed/profile overlay).

## Loop Control
STOP
